services:
  autodw-app:
    build: .
    ports:
      - "8003:8002"  # CHANGED - now both are 8002
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    environment:
      - PORT=8002
      - HOST=0.0.0.0
    restart: always
    container_name: autodw-production